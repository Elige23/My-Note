<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout for a single subtask item in RecyclerView in TaskEntryBottomSheetDialog when creating group task.

    SubtaskEntryBottomSheetAdapter: adapter for managing subtask list.

    Components:
    - MaterialCheckBox: is for illustrative purposes only (non-interactive, clickable="false").
      Serves as a visual indicator only, not for marking subtasks as completed.
    - EditText: subtask description (editable inline).
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="8dp"
    android:background="@drawable/border"
    android:orientation="horizontal"
    android:paddingVertical="4dp">

    <com.google.android.material.checkbox.MaterialCheckBox
        android:id="@+id/checkBoxSubTask"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:clickable="false"
        android:padding="5dp"
        app:buttonTint="@color/secondary" />

    <EditText
        android:id="@+id/descSubTask"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:imeOptions="actionNext"
        android:inputType="text"
        android:minHeight="30dp"
        android:padding="8dp" />
</LinearLayout>